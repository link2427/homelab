apiVersion: apps/v1
kind: Deployment
metadata:
  name: cosmotrak
  namespace: cosmotrak
spec:
  replicas: 1
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 300
  selector:
    matchLabels:
      app.kubernetes.io/name: cosmotrak
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cosmotrak
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: site
          image: ghcr.io/link2427/cosmotrak-website@sha256:45298945cbe7120906c914d3f822de0d2ff84e654909bf795e228716f937ba5e
          ports:
            - name: http
              containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 20
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "8080"
            - name: SATELLITE_STATUS_URL
              value: http://satellite-data-status.satellite-data-prod.svc.cluster.local
---
apiVersion: v1
kind: Service
metadata:
  name: cosmotrak
  namespace: cosmotrak
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cosmotrak
  ports:
    - name: http
      port: 80
      targetPort: http

