apiVersion: v1
kind: Service
metadata:
  name: coder-postgres
  namespace: coder
  labels:
    app.kubernetes.io/name: coder-postgres
    app.kubernetes.io/part-of: coder
spec:
  selector:
    app.kubernetes.io/name: coder-postgres
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: coder-postgres
  namespace: coder
  labels:
    app.kubernetes.io/name: coder-postgres
    app.kubernetes.io/part-of: coder
spec:
  serviceName: coder-postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: coder-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: coder-postgres
        app.kubernetes.io/part-of: coder
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres
          image: postgres:17.9-alpine
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          env:
            - name: POSTGRES_DB
              value: coder
            - name: POSTGRES_USER
              value: coder
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: coder-secrets
                  key: postgres-password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          startupProbe:
            exec:
              command: [pg_isready, -U, coder, -d, coder]
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            exec:
              command: [pg_isready, -U, coder, -d, coder]
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command: [pg_isready, -U, coder, -d, coder]
            periodSeconds: 30
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app.kubernetes.io/name: coder-postgres
          app.kubernetes.io/part-of: coder
          recurring-job.longhorn.io/source: enabled
          recurring-job.longhorn.io/coder-db-backup: enabled
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: longhorn-resilient
        resources:
          requests:
            storage: 10Gi
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: coder-postgres
  namespace: coder
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: coder-postgres
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: coder
      ports:
        - port: 5432
          protocol: TCP
---
# Internal split-horizon endpoint: workspace Pods resolve this Service while
# Tailscale clients resolve the same short hostname through MagicDNS.
apiVersion: v1
kind: Service
metadata:
  name: olympus-coder
  namespace: coder
  labels:
    app.kubernetes.io/name: coder
    app.kubernetes.io/part-of: coder
spec:
  selector:
    app.kubernetes.io/name: coder
    app.kubernetes.io/instance: coder
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
