# September 12 validated database release

Runtime source `218cfc820b6f4480da41bceb3162784906b29441` is on backend `dev` and
`main`. Linux CI [34723529186](https://github.com/link2427/SatelliteDataApi/actions/runs/34723529186)
passed .NET, Python, fixture-contract and restricted-runtime checks. Both Olympus
environments pin:

```text
ghcr.io/link2427/satellite-data-api@sha256:8c0de5cd469ded4dbaccd551e4dce5d5a69c083efe063484f4e207334a535182
```

The snapshot was exported by `51959d389cf9c3ab2d37f3ba915b94d1e2b3c0b0`.
Its manifest correctly retains that export provenance; publisher configuration
uses the deployed commit for future scheduled exports.

```text
SHA-256 cad124e004fce63652fb4e53df990fc43b65bfc8606fdba5bd95c72b209b3c2d
18,391,040 bytes
Horizons source generation: 2026-09-12T22:22:49Z
Export completed: 2026-09-12T22:26:44.743043+00:00
```

One genuine Horizons capture made 36 requests, including one bounded MRO retry.
Offline replay with OS network denial reproduced the identical exported bytes.
A missing-Moon fixture failed and preserved all Horizons tables and launch state.
Shipped 1.4, 1.4.1 and 1.5 SQL/date/vector helper tests each accepted all 4,898
body/spacecraft vectors. The modern backend validation bundle also passed.
This is genuinely regenerated data, followed by reuse-only storage publication.
No additional Horizons, CelesTrak or Launch Library requests ran during rollout.

The database contains 14,618 satellites, 8,001 launches, 2,123 body samples,
12,010 orbit samples and 2,775 spacecraft samples. All retained 11 bodies,
10 orbit targets and 15 spacecraft remain. Frames are
`ICRF/J2000 equatorial; UTC; v2`, legacy CalendarDateTdb fields are null and
sample epochs use whole-second UTC ISO text. Normal short coverage is September
9–October 27; MRO has 73 freshly regenerated samples through September 27.

## Development

The existing private `cosmotrak-data-dev` R2 bucket received
`Satellite_Database/satellite-database-2026-09-12.db` at 22:43:02 UTC.
An independent full download matched the snapshot hash. Dev's working database,
original offset-5300 launch cursor, two cache/cooldown files, bootstrap data and
ingestion schedule remained unchanged. Only its candidate/baseline publication
copies advanced. The CronJob remains suspended.

Verified backup: `/data/dev/backups/before-release-20260912T224250751903Z`.
The original September 10 pre-R2 migration backup and off-PVC archive remain.
The one-off `satellite-release-dev-20260912` Job is retired after success.

## Production

The one-off `satellite-release-prod-20260912` Job completed at 22:55:49 UTC.
It held the publisher lock while verifying/backing up current state and replacing
only the five Horizons tables in the existing working database. It first rejected
any mismatch in non-Horizons rows against the captured input; the offline rehearsal
proved that an advanced launch cursor stops staging without mutation.

All non-Horizons table rows and all 39 provider cache files are preserved. The
current launch cursor remains `limit=100&ordering=-net`, updated September 12
04:53:48 UTC. The working database now holds the corrected Horizons data for
future incremental ingestion. PVC UID `f4479c09-b719-4f63-8871-5a6f3415aec1`
and existing S3/registry credentials are unchanged. The new bucket-scoped R2
credential is SOPS encrypted and mounted only by publishers, not status pods.

Independent downloads verified identical bytes from both destinations:

- S3: `cosmotrak-data/Satellite_Database/satellite-database-2026-09-12.db`.
- R2: `cosmotrak-data-prod/v1/databases/<snapshot-sha256>.db`.
- Public manifest: https://data.cosmotrak.com/v1/manifest.json.

The manifest was published only after verifying the immutable public download.
The [edge service](../edge/README.md) allows GET/HEAD only for the manifest and
content-hash database paths. Native, Python and no-user-agent requests pass
without cookies, login, redirects or browser challenges. The manifest requires
revalidation and returns 304 for matching ETags; database responses preserve
exact size/bytes and immutable caching. Private paths and writes are denied.
Both R2 buckets retain disabled managed public access; Workers development and
preview URLs are disabled. Worker version:
`4f2eb0fe-88b4-4a4b-94c8-a759cee21932`.

Both environments returned HTTP 200 for `/health/live`, `/status` and
`/health/data`, with `Cache-Control: no-store`. Freshness is based on authentic
satellite data from September 12 04:53:36 UTC, not the later publication time.
Dev will become stale unless a further controlled refresh is authorized.

Production resumes the existing 15-minute check schedule. Its next ingestion
deadline remains September 13 04:55:01.502334 UTC; no scheduler/cooldown reset
was used. Delivery retries use the saved candidate and never rerun ingestion.

## Rollback and evidence

Verified production PVC backup:
`/data/prod/backups/before-release-20260912T225457892550Z`.
The original September 12 S3 bytes (20,443,136 bytes, SHA-256 `62f0b7136b539080f3d8c4b2372aef2653d71bae86e76f5b61815e582481150c`)
remain in the same bucket at
`Satellite_Database/Release_Backups/2026-09-12/62f0b7136b539080f3d8c4b2372aef2653d71bae86e76f5b61815e582481150c.db`.
The copy was independently downloaded and verified September 12 at 23:10 UTC.
Backups must stay outside `Satellite_Database/satellite-database`: shipped readers
accept any `.db` under that prefix and may fall back to LastModified, even though
the publisher itself uses stricter date-only discovery.

**Cleanup pending:** the newly created duplicate
`Satellite_Database/satellite-database-2026-09-12-before-62f0b7136b539080f3d8c4b2372aef2653d71bae86e76f5b61815e582481150c.db`
still needs deletion by an AWS management identity. The publisher has Get/Put
access within `Satellite_Database/*` and no DeleteObject permission. Its credentials
and permissions were not broadened. Delete only this duplicate after checking
the verified replacement above; preserve all original dated releases. The
supplemental `s3-rollback-relocation.json` draft release asset records the copy,
exact ETags, discovery listing and deletion blocker. The original production
receipt remains the historical publication record, not proof of cleanup.
Earlier S3 objects, the dev S3 rollback bucket, PVCs and Longhorn backups remain.

Before rollback suspend the production CronJob and wait for the writer to stop.
Preserve current state again. Restore database/metadata coherently from the
verified backup if needed, and remove `modern` from configuration before using
the prior S3-only image. Do not shorten current provider holds or replace newer
launch cursors with the dev cursor. Retain both delivery paths and snapshots;
retries are reuse-only. Never delete or recreate either PVC.

Private draft release `backend-release-validation-20260912` in SatelliteDataApi
contains `dev-release-receipt.json` and `prod-release-receipt.json` alongside the
actual source/replay and native-reader evidence. Windows coordination evidence
is under `.ai-context/satellite-release-20260912`; it includes independently
downloaded databases, live inventories, protected-state hashes and job logs.
The source-capture task maintains the final backend validation bundle.
