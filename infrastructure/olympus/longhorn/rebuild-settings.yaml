apiVersion: longhorn.io/v1beta2
kind: Setting
metadata:
  name: concurrent-replica-rebuild-per-node-limit
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
# Atlas shares its system disk with image unpacking. Serial recovery avoids
# competing rebuilds amplifying disk latency and causing more replica timeouts.
value: "1"
