apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: olympus-app-backup
  namespace: longhorn-system
spec:
  cron: "0 2 * * *"
  task: backup
  retain: 7
  concurrency: 1
  groups: []
  labels:
    olympus.dev/tier: application-state
  parameters:
    full-backup-interval: "7"
